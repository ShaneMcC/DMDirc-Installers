#!/bin/sh

if [ "remove" = "${1}" -o "upgrade" = "${1}" -o "deconfigure" = "${1}" ]; then
	if [ -e "/usr/share/services/irc.protocol" ]; then
		update-alternatives --quiet --remove irc.protocol /opt/dmdirc/irc.protocol
	fi;

	# TODO: Also remove from gnome
fi;

#DEBHELPER#

exit 0;