#!/bin/sh

if [ "configure" = "${1}" ]; then
	if [ -e "/usr/share/services/" ]; then
		update-alternatives --quiet --install /usr/share/services/irc.protocol irc.protocol /opt/dmdirc/irc.protocol 50
	fi;

	# TODO: Also add to gnome
fi;

#DEBHELPER#

exit 0;